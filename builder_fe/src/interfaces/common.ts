export enum Locale {
  EN = 'en',
  ES = 'es',
}

export interface SVGProps {
  color?: string;
}

export enum EDevices {
  desktop = 'desktop',
  mobile = 'mobile',
}

export enum ESideBarActive {
  session = 'session',
  setting = 'setting',
}
